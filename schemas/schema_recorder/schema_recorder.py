# schema_recorder Pydantic model
# def assign_stream_segment_id(user_id: str, timestamp: datetime) -> str:
# Round to 10-second windows or use a UUID per session chunk
# return f"{user_id}_{timestamp.strftime('%Y%m%dT%H%M%S')}"
