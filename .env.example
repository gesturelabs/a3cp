# Django settings
DEBUG=False
SECRET_KEY=changeme-in-prod
ALLOWED_HOSTS=gesturelabs.org,www.gesturelabs.org

# Database settings (if using PostgreSQL later)
DB_NAME=a3cp
DB_USER=a3cp_user
DB_PASSWORD=changeme
DB_HOST=localhost
DB_PORT=5432

# FastAPI settings (to be used later)
INFER_API_URL=http://127.0.0.1:9000/api/infer/

# Cloud storage (optional later)
CLOUD_STORAGE_KEY=your-cloud-key
CLOUD_STORAGE_SECRET=your-cloud-secret

# ------------------------------------------------------------------
# A3CP Server Port Configuration
# ------------------------------------------------------------------
GUNICORN_PORT=8000     # Port for Django/Gunicorn (WSGI)
UVICORN_PORT=8001      # Port for FastAPI/Uvicorn (ASGI)    # Dev default; override in real .env (e.g., 9000 for prod)GUNICORN_PORT=8000
UVICORN_PORT=8001
